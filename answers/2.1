# Create docker-compose.yml file
echo "version: '3'
services:
  mlops-backend:
    image: jrpythagore20/coursesilv2024dockerhub:mlops-backend" > docker-compose.yml

# Start the back service using docker-compose
docker-compose up -d