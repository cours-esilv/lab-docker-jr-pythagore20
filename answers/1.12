# Start the Back-end Container
docker run -d -p 5000:5000 --name backend jrpythagore20/coursesilv2024dockerhub:mlops-backend

# Wait for a moment to ensure backend is up (you can optimize this later)
sleep 5

# Start the Front-end Container Pointing to the Back-end
docker run -d -p 8080:80 --name frontend --env WS_BACK_URL=http://localhost:5000 jrpythagore20/coursesilv2024dockerhub:mlops-frontend
